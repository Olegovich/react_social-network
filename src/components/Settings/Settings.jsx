import React from "react";
import s from "./Settings.module.css";

const Settings = () => {
    return (
        <main>
            <div>
                Settings
            </div>
        </main>
    );
};

export default Settings;
